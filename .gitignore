# The two project repos carry their own git history and remotes.
# This repo tracks only the shared working context.
/tradestack/
/frontend/

# Postman exports can carry live session tokens - keep them out of git.
/Postman/
